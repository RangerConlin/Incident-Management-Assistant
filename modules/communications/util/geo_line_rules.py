def is_line_a_applicable(row: dict) -> bool:
    """Placeholder for Line A applicability checks.

    Future implementations may use geographic boundary data to determine if
    the given row requires coordination.  Currently always returns ``False``.
    """
    return False


def is_line_c_applicable(row: dict) -> bool:
    """Placeholder for Line C applicability checks."""
    return False
