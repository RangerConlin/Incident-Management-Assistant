"""Incident Map window package.

Ribbon-UI (Office-style tabs/collapsible groups) + Leaflet map canvas + a
collapsible bottom panel, built on top of the shared modules/gis package
(feature/layer registries, spatial_repository, geometry_service). See
Design Documents/Instructions for the broader GIS architecture; this
sub-package only concerns itself with the window chrome, ribbon tool UI, and
map interaction wiring.
"""
