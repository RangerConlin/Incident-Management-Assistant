# Incident Management Assistant — Claude Instructions

@agents.md

This repo is touched by multiple AI agents/tools (Codex, Claude Code, etc.).
`agents.md` is the single source of truth for project facts, architecture,
hard rules, and migration status — it is imported above, not duplicated here.

Do not re-add project content to this file. If you learn something about the
codebase that should persist, edit `agents.md` directly so every agent sees
it, not just Claude Code sessions.
